// Write a function that maps an array of numbers to their squares using map().

// The function should take an array of numbers as input and return a new array with the squares of
let numbers = [1,2,3,4,5,6];
let squares = numbers.map(num => num ** 2);
console.log(sum);